<%- include("templates/header") %>
<div class="mt-5 mx-5">
    <h1 style="font-family:Bree Serif;">USERS</h3>
        <div class="container mt-3">
            <% for (let i=0; i<users.length; i++) { %>
                <%- include("templates/user", {user: users[i]}) %>
            <% } %>
    </div>
</div>
<%- include("templates/footer") %>